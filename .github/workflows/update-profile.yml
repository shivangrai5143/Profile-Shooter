name: Update Profile Space Shooter

on:
  schedule:
    # Runs every Monday at 6:00 AM IST
    - cron: '30 0 * * 1'
  
  # Allows manual trigger from Actions tab
  workflow_dispatch:

jobs:
  update-profile:
    runs-on: ubuntu-latest
    
    steps:
      # Step 1: Checkout this repository (contains the Python code)
      - name: Checkout this repo
        uses: actions/checkout@v4

      # Step 2: Set up Python environment
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      # Step 3: Install dependencies
      - name: Install dependencies
        run: pip install -r requirements.txt

      # Step 4: Generate the game GIF
      - name: Generate game GIF
        env:
          GH_TOKEN: ${{ secrets.GH_PAT }}
        run: python generate_gif.py

      # Step 5: Clone profile repo, update ONLY the GIF file, and push
      # NOTE: This does NOT modify your README.md - only the game.gif file!
      # Your existing README content is fully preserved.
      - name: Update profile repository
        env:
          GH_PAT: ${{ secrets.GH_PAT }}
        run: |
          # Clone your profile repository
          git clone https://x-access-token:${GH_PAT}@github.com/shivangrai5143/shivangrai5143.git profile-repo
          
          # Copy the generated GIF to profile repo (replaces old GIF only)
          cp game.gif profile-repo/
          
          # Configure git
          cd profile-repo
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git config user.name "github-actions[bot]"
          
          # Commit and push (only if the GIF has changed)
          git add game.gif
          git diff --staged --quiet || git commit -m "🚀 Update space shooter game GIF"
          git push

